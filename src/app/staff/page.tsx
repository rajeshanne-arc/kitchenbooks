// The group's first tab, RENDERED HERE rather than redirected to — one
// server round trip instead of two, same as every chip parent.
export const dynamic = 'force-dynamic'
export { default } from './people/page'
