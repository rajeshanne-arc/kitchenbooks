# Feature delivery ledger

This is the implementation tracker. “Existing” means code is present, not that
the feature is production-complete. Each workstream gets its own acceptance
tests before being marked complete.

| Workstream | Current state | Target | Next slice |
| --- | --- | --- | --- |
| Identity and restaurant membership | Existing single-restaurant auth; password change; compatibility migration; switching and membership lifecycle implemented; owner-issued one-time password recovery; secure one-use 48-hour invitation links; local restricted-role isolation acceptance passes | Apply migration, verify provisioning endpoint, invitations and recovery | Shared migration verification and real-user acceptance |
| Tenant security | Strong server-side tenant context and RLS audits | Preserve isolation through membership/switching | Membership security tests |
| Purchasing | Vendors, POs, receipts, returns, partial fulfilment, vendor statements, approval state machine, quotation register, dated quote decisions, accepted-quote-to-draft-PO conversion, and append-only invoice match assessments implemented behind migrations | Review UI for invoice exceptions and acceptance tests | Apply migrations and execute cross-role purchasing acceptance tests |
| Inventory | Items, locations, expiry, valuation, counts, adjustments, split-location transfer ledger, and FEFO-ready append-only lot ledger implemented behind migrations; receipts, issues, returns, wastage, purchase reversals, adjustments, and transfers carry lot/location movements; current aggregate stock is carried as labelled legacy lots; unplaced lots remain issuable but cannot be transferred without a source location | Historical lot reconciliation | Apply migrations and execute inventory acceptance tests |
| Recipes and kitchen | Recipe and production workflows exist; immutable version/effective-date snapshots and history panel implemented; policy-aware mapped-POS prep-demand list, production records pinned to effective recipe versions, expected output plus measured waste, monthly variance review with manager/owner acknowledgement or correction request, recipe-line substitutions with ratio/note, and immutable void-and-re-file correction path are implemented behind migrations; production outputs remain a separate kitchen ledger by approved decision | Version-aware planning follow-through | Apply migrations and verify version history, substitutions, variance review, prep demand, and corrective void/re-file across every edit path |
| Sales/Petpooja | Manual/Petpooja adapter, local demo mode, durable sync attempts, bounded transient retries, fail-closed response-envelope validation, failure visibility, latest-fetch idempotency, atomic POS revenue journal with payment-mode mappings, encrypted per-tenant credentials, hourly cron worker, explicit POS receivable confirmation queue, append-only source generations, immutable provider-statement comparison, manager/accountant POS difference review records, and owner-controlled none/reconcile POS stock policy | Provider-account verification and correction execution | Validate the documented contract against a live provider export, then execute POS acceptance tests |
| Accounting | Cash, books, expenses, reconciliation foundations; prepared immutable double-entry journal, configurable tenant-scoped chart of accounts, explicit posting-mapping layer, atomic vendor-payment, purchase, expense, other-income, voucher, payroll, POS posting/reversal, balanced-posting function, closed-period guard, trial balance, journal-backed P&L, cumulative balance sheet, direct mapped-cash-flow screen, indirect working-capital cash-flow bridge, idempotent recurring-entry templates, accrual/reversal workflow, and fixed-asset depreciation register | Complete source corrections, opening balances, and historical backfill | Apply journal migrations, configure chart/mappings, then execute historical backfill and all acceptance tests |
| Payroll | Labour/payroll foundations, three-stage run control, frozen lines, journal payment, printable payslip statements, effective-dated salary structures, identifier-aware frozen-run CSV export, provider-neutral withholding export, explicit paid/unpaid holiday calendar, effective-dated statutory configuration, configured PF/ESI contribution calculations, assigned leave-policy allowances, manager/owner leave request approval that writes approved attendance, and accountant/owner year-end leave carry-forward ledger implemented behind migrations | Reviewed provider-specific TDS filing contract | Add reviewed TDS provider contract |
| Documents | Bill-photo and meter-reading evidence upload/list/read flows, private Vercel Blob adapter, tenant-scoped keys, authenticated streaming route, forced-RLS migration, and manager/owner archive metadata implemented behind migrations | Production Blob credentials and external retention execution | Configure production Blob store and execute retention policy acceptance tests |
| SOPs/localization | Role operating guides with written moments, live route links, print layout, Telugu prose for all 17 role moments, and English/Telugu staff-form labels | Keep copy aligned when workflows change | Re-run the content and route gates for future SOP edits |
| Imports | Validated opening-stock, POS statement, staff, vendor, item, sales, payroll, chart-account, balanced opening-balance, one-bill purchase, and reusable multi-bill purchase CSV imports; every supported import has a write-free complete-file preview and explicit commit gate; batch commits preserve source bill references and use one atomic normal-domain transaction | Keep source-specific historical reconciliation as a separate evidence gate | Run the batch preview against the approved source export, then perform live acceptance |
| Production readiness | Build/lint/security audits exist; Next.js/sharp advisories patched; recovery and migration boundaries documented; deployment preflight rejects demo mode, local TLS disablement, missing secrets, invalid storage credentials, and database-owner URLs; consolidated restricted-role local gates and disposable historical fixture pass | Shared-database migration, live-lot reconciliation, external integrations, and irreversible production deployment approval | Execute the live release runbook after external dependencies are supplied |

Migration execution order and the database-owner boundary are recorded in
[`docs/migration-runbook.md`](migration-runbook.md). No pending migration is
silently applied by the application role.
